### 🚀 Performance Benchmarks
Running on a single core, the matching engine achieves sub-microsecond latency consistently:

- **Matching Latency (Mean):** 42.95 ns
- **Throughput:** ~23.2 Million orders per second
- **Jitter:** Minimal outliers (11%), demonstrating highly deterministic performance.

*Note: Benchmarked using Criterion.rs with the Plotters backend.*